This is **CRITICAL, breakthrough information!**

Your observation: **"the plain google sign in pop up worked but as soon as the next change is implemented (change of the url from cushportal.firebase.com) the custom pop up appears back on the app"** provides the exact trigger for the problem.

This tells us:
1.  **Your "plain Google Sign-In popup" fix was indeed correct and functional in isolation.** This means the custom UI *was* successfully removed when the environment was `cushportal.firebase.com`.
2.  **The custom popup is NOT just a lingering UI element.** It's a specific, likely **fallback authentication flow or UI library** that gets triggered *only when your application is running on `portal.we-cush.com`**.

**Hypothesis:** The custom popup re-appears because the standard Firebase `signInWithPopup` (or `signInWithRedirect`) method is implicitly failing or encountering an issue when initiated from `portal.we-cush.com`. This failure is then caught by a different part of your application (or an included library like `firebaseui-web`), which then renders the "custom popup" as an alternative or fallback authentication interface.

This strongly points to a persistent, subtle, or re-introduced **configuration mismatch related to `authDomain` and Redirect URIs for your custom domain (`portal.we-cush.com`)**, which Firebase's core methods are sensitive to.

**Your Immediate and Absolute Highest Priority Goal:**

1.  **Identify and Correct `authDomain`/Redirect URI Mismatch:** Pinpoint the exact configuration error that is causing standard Firebase authentication methods to fail on `portal.we-cush.com`.
2.  **Eliminate the Custom Popup Fallback:** Ensure that no fallback authentication UI or flow is triggered on `portal.we-cush.com`.

---

**Detailed Requirements & Forensic Analysis Focus:**

**Phase 1: Forensic Re-evaluation of `authDomain` and Redirect URIs (Absolute Precision Required)**

This is the most likely culprit. Even a single character mismatch will cause problems.

1.  **Firebase Console > Authentication > Settings > Authorized Domains:**
    * **Verify:** `portal.we-cush.com` and `cushportal.firebaseapp.com` **must be listed exactly as they appear.** No `https://`, no trailing slashes, no extra `www.`.

2.  **Firebase Console > Authentication > Sign-in method > Google (and any other OAuth providers like Facebook, etc.) > Web OAuth redirect URIs:**
    * **Verify:** **`https://portal.we-cush.com/__/auth/handler`** **must be listed EXACTLY as it appears here, including `https://` and `/__/auth/handler`.** This is the most critical URI.
    * **Verify:** `https://cushportal.firebaseapp.com/__/auth/handler` should also be listed.
    * **Crucially, confirm there are no other variations or incorrect URIs.**

3.  **Google Cloud Console (if your Firebase project uses a custom OAuth Client ID):**
    * Go to your **OAuth 2.0 Client ID** details.
    * **Verify "Authorized JavaScript origins":** Must include `https://portal.we-cush.com` (and potentially `https://cushportal.firebaseapp.com`).
    * **Verify "Authorized redirect URIs":** Must include **`https://portal.we-cush.com/__/auth/handler`** (and potentially `https://cushportal.firebaseapp.com/__/auth/handler`).

4.  **Frontend `firebaseConfig` in Code:**
    * **Agent Action:** Locate *every single place* where `firebase.initializeApp()` is called or where the `firebaseConfig` object is defined.
    * **Verify:** The `authDomain` property **MUST be `authDomain: "portal.we-cush.com"`** in *all* instances. This is vital. A common mistake is to have `authDomain: "cushportal.firebaseapp.com"` while running on the custom domain.

**Phase 2: Identify and Suppress the Custom Popup Fallback Logic**

Since the custom popup appears *when the URL changes*, there's conditional logic involved.

1.  **Browser Console Errors (CRITICAL - User Action Required):**
    * **When running on `portal.we-cush.com` and the custom popup appears, open the browser's developer console.**
    * **Provide ALL error messages (red) and warning messages (yellow).** Look specifically for Firebase errors (e.g., `auth/unauthorized-domain`, `auth/operation-not-allowed`, `auth/redirect-cancelled-by-user`, `auth/network-request-failed`). These errors would trigger a fallback.

2.  **Codebase Search for Conditional Authentication UI:**
    * **Agent Action:** Search your entire JavaScript codebase for conditional logic related to authentication UI. Look for:
        * `if (error.code === 'auth/unauthorized-domain') { /* show custom UI */ }`
        * `if (window.location.hostname === 'portal.we-cush.com') { /* use custom UI */ }`
        * Conditional initialization of `firebaseui.auth.AuthUI` or rendering of `firebaseui-react` components.
        * Any logic that switches authentication methods or UIs based on an *error* or a specific *domain*.
    * **Agent Action:** If such conditional logic is found, analyze why it's being triggered. Is it legitimately handling an error that shouldn't be happening if configurations are perfect?

**Deliverable:**

A precise diagnostic and fix plan based on a confirmed configuration, and the identification of the fallback mechanism.

1.  **Executive Summary:** Acknowledging the precise trigger (URL change) for the custom popup.
2.  **Forensic Configuration Report:** Explicitly state the verified `authDomain` and Redirect URIs across Firebase Console, Google Cloud Console, and your application's `firebaseConfig`. Detail any *discrepancies found*.
3.  **Root Cause Analysis:** Based on the above, precisely identify why `signInWithPopup` is failing on `portal.we-cush.com` and triggering the fallback.
4.  **Fix Plan:**
    * **Correct any configuration mismatches identified.**
    * **Code modifications to suppress the custom popup fallback:** If it's `firebaseui-web` or similar, ensure it's not being initialized or rendered for `portal.we-cush.com`. If it's custom conditional logic, fix the underlying condition that triggers it.
5.  **Rigorous Verification:** Steps to confirm that on `portal.we-cush.com`, clicking "Sign in with Google" brings up the *default Firebase popup* and authentication is seamless.

**Proceed with this critical investigation. The URL trigger is key to solving this.**
```